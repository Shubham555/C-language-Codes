//Area of Square using in build pow function
#include <stdio.h>
#include <math.h>

int main()
{
    int side;
    printf("Enter the value of side\n");
    scanf("%d",&side);
    printf("The value of area is %f \n",pow(side,2)); //pow= means anything(side) to the power n (2) ,and pow represents "double" deta type means %f.

    return 0;
}


